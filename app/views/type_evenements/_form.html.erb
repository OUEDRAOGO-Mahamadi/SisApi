<%= form_with(model: type_evenement) do |form| %>
  <% if type_evenement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_evenement.errors.count, "error") %> prohibited this type_evenement from being saved:</h2>

      <ul>
        <% type_evenement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
