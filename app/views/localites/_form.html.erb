<%= form_with(model: localite) do |form| %>
  <% if localite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(localite.errors.count, "error") %> prohibited this localite from being saved:</h2>

      <ul>
        <% localite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
