<%= form_with(model: region_localite) do |form| %>
  <% if region_localite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(region_localite.errors.count, "error") %> prohibited this region_localite from being saved:</h2>

      <ul>
        <% region_localite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :localite_id %>
    <%= form.text_field :localite_id %>
  </div>

  <div class="field">
    <%= form.label :region_id %>
    <%= form.text_field :region_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
