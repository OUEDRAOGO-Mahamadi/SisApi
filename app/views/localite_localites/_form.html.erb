<%= form_with(model: localite_localite) do |form| %>
  <% if localite_localite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(localite_localite.errors.count, "error") %> prohibited this localite_localite from being saved:</h2>

      <ul>
        <% localite_localite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :localite_id %>
    <%= form.text_field :localite_id %>
  </div>

  <div class="field">
    <%= form.label :sous_localite_id %>
    <%= form.text_field :sous_localite_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
