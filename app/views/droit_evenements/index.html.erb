<p id="notice"><%= notice %></p>

<h1>Droit Evenements</h1>

<table>
  <thead>
    <tr>
      <th>Libelle</th>
      <th>Creer</th>
      <th>Lire</th>
      <th>Modifier</th>
      <th>Supprimer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @droit_evenements.each do |droit_evenement| %>
      <tr>
        <td><%= droit_evenement.libelle %></td>
        <td><%= droit_evenement.creer %></td>
        <td><%= droit_evenement.lire %></td>
        <td><%= droit_evenement.modifier %></td>
        <td><%= droit_evenement.supprimer %></td>
        <td><%= link_to 'Show', droit_evenement %></td>
        <td><%= link_to 'Edit', edit_droit_evenement_path(droit_evenement) %></td>
        <td><%= link_to 'Destroy', droit_evenement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Droit Evenement', new_droit_evenement_path %>
