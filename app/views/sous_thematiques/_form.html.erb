<%= form_with(model: sous_thematique) do |form| %>
  <% if sous_thematique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sous_thematique.errors.count, "error") %> prohibited this sous_thematique from being saved:</h2>

      <ul>
        <% sous_thematique.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
